{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/partment/opencode-openai-compact/main/configSchema.json",
  "title": "opencode-openai-compact configuration",
  "description": "Configuration schema for the opencode-openai-compact OpenCode plugin.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON schema URL for editor integration."
    },
    "enabled": {
      "type": "boolean",
      "default": true,
      "description": "Enables or disables OpenAI Responses API compaction checkpoint injection."
    },
    "providers": {
      "type": "object",
      "minProperties": 1,
      "default": {
        "openai": {
          "compactModel": "gpt-5.4"
        }
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["compactModel"],
        "properties": {
          "compactModel": {
            "type": "string",
            "minLength": 1,
            "description": "Model sent to the OpenAI Responses API compaction v2 request for this provider."
          }
        }
      },
      "description": "OpenCode provider ids to wrap, keyed by provider id."
    },
    "headers": {
      "type": "object",
      "additionalProperties": false,
      "default": {},
      "properties": {
        "compact": {
          "type": "string",
          "minLength": 1,
          "default": "x-opencode-openai-responses-compact",
          "description": "Internal request header used to mark compaction calls."
        },
        "session": {
          "type": "string",
          "minLength": 1,
          "default": "x-opencode-openai-responses-compact-session",
          "description": "Internal request header used to pass the OpenCode session id into fetch."
        }
      }
    },
    "responses": {
      "type": "object",
      "additionalProperties": false,
      "default": {},
      "properties": {
        "endpointPath": {
          "type": "string",
          "minLength": 1,
          "default": "/responses",
          "description": "Responses API endpoint path suffix to intercept."
        },
        "compactEndpointPath": {
          "type": "string",
          "minLength": 1,
          "default": "/responses/compact",
          "deprecated": true,
          "description": "Deprecated compatibility field. Compaction v2 always uses endpointPath."
        }
      }
    },
    "compactBodyKeys": {
      "type": "array",
      "default": [
        "input",
        "instructions",
        "tools",
        "parallel_tool_calls",
        "reasoning",
        "service_tier",
        "prompt_cache_key",
        "text"
      ],
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Request body keys copied from the original responses request into the compaction v2 request."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "default": "Context compacted.\nFollowing conversations will continue from this compacted checkpoint.",
      "description": "Synthetic assistant text shown after a successful compact call."
    },
    "state": {
      "type": "object",
      "additionalProperties": false,
      "default": {},
      "properties": {
        "retentionDays": {
          "type": "integer",
          "minimum": 1,
          "default": 30,
          "description": "Number of days to keep compaction checkpoints."
        },
        "deleteOnSessionDeleted": {
          "type": "boolean",
          "default": true,
          "description": "Deletes checkpoints for a session when OpenCode emits session.deleted."
        }
      }
    }
  }
}
